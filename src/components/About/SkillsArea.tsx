import SkillArea from './SkillArea';
import { SkillsContainer } from './styles';

function SkillsArea() {
  return (
    <SkillsContainer>
      <SkillArea text="Frameworks" />
      <SkillArea text="Frameworks" />
      <SkillArea text="Frameworks" />
      <SkillArea text="Frameworks" />
    </SkillsContainer>
  );
}

export default SkillsArea;
